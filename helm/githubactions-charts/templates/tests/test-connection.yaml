apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "githubactions-charts.fullname" . }}-test-connection"
  labels:
    {{- include "githubactions-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "githubactions-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
